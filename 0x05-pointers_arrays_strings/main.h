/**
 * reset_to_98 - change the value that the pointer points to
 * @n: the input pointerto integer
 */
void reset_to_98(int *n);
/**
 * swap_int - function that swaps the values of two integers.
 * @a: the first argument
 * @b: the second argument
 */
void swap_int(int *a, int *b);
/**
 * _strlen - a function that returns the length of a string.
 * @s: the pointer to char
 * Return: the length of string
 */
int _strlen(char *s);
/**
 * _puts -  a function that prints a string, followed by a new line, to stdout
 * @str: the pointer to char
 */
void _puts(char *str);
 /**
 * print_rev -  a function that prints every other character of a string, starting with the first character, followed by a new line.
 * @s: the pointer to char
 */
void print_rev(char *s);
/**
 * rev_string - a function that reverses a string.
 * @s: the pointer to char
 */
void rev_string(char *s);
/**
 * puts2 -  a function that prints every other character of a string, starting with the first character, followed by a new line.
 * @str: the pointer to char
 */
void puts2(char *str);
/**
 *  puts_half - a function that prints half of a string, followed by a new line.
 * @str: the pointer to char
 */
void puts_half(char *str);
/**
 * print_array -  a function that prints n elements of an array of integers, followed by a new line.
 * @a: the pointer to int
 * @n: input integer
 */
void print_array(int *a, int n);
/**
 * _strcpy - function that copies the string pointed to by src, including the terminating null byte (\0), to the buffer pointed to by dest.
 * @dest: the pointer to char
 * @src: the pointer to char
 * Return:  the pointer to dest
 */
char *_strcpy(char *dest, char *src);
/**
 * _atoi - a function that convert a string to an integer.
 * @s: the pointer to char
 * Return: the integer value of input string
 */
int _atoi(char *s);
/**
 * _putchar - writes the character c to stdout
 * @c: The character to print
 *
 * Return: On success 1.
 * On error, -1 is returned, and error is set appropriately.
 */

int _putchar(char c);
